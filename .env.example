# 手元で動かすときの環境変数のひな形。
# これをコピーして .env を作り、値を入れる。
#
#     cp .env.example .env
#
# .env は .gitignore で除外してある。コミットしないこと。
# GitHub Actions では .env は使わず、GitHub Secrets から渡す。

# AI要約に使う Claude API のキー（P4）。
# https://console.anthropic.com/ で発行する。
# 発行したら、同じコンソールで利用上限額を必ず設定しておくこと。
ANTHROPIC_API_KEY=
