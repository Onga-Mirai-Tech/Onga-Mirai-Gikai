# 依存は必要なものだけに絞る（docs/設計ドラフト.md「依存ライブラリ」）。
# 会議録の取得と解析（pipeline/fetch, pipeline/parse/transcript 等）は
# 標準ライブラリだけで動く。

# PDF解析。一般質問通告書と審議案件・結果は PDF でしか公開されていない。
pypdf==6.1.3

# Claude API（P4のAI要約）。Batch API を使うため（2026-09-23 決定）。
# バッチの作成・状態確認・結果JSONLの取得と、混雑時の再試行を公式実装に任せる。
anthropic==1.8.0
