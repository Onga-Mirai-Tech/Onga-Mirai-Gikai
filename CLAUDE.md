# CLAUDE.md — みらい議会 -遠賀町版-

遠賀町議会の会議録をもとに、議事の流れと「誰が何を言い、町がどう答えたか」をわかりやすく見せる**個人運営の非公式サイト**。
詳細設計は `docs/設計ドラフト.md` を参照すること。

## プロジェクトの前提

- 公開URL: `https://miraigikai.onga-mirai-tech.com`（エックスサーバーのサブドメイン）
- 立場: 個人の非公式サイト。遠賀町・遠賀町議会・「チームみらい」とは無関係。
- 参考: みらい議会（福岡県版）`https://github.com/bakumon1107/mirai-gikai-fukuoka-pref`
  - **見た目・言葉づかいの参考のみ。コードは流用しない**（AGPL-3.0 のため）。
- データ源: 遠賀町議会 会議録検索システム（VOICES）`http://iasb-sv.town.onga.lg.jp/voices/index.asp`

## アーキテクチャ（プランB：静的サイト）

- 収集・整形・AI要約・サイト生成はすべて **GitHub Actions 上のパイプライン**で行う。
- 公開物は **HTML / CSS / JS / JSON のみ**。実行時のサーバー処理・DB・PHPは使わない。
- デプロイ先はエックスサーバー（SFTP）。
- AIチャット機能は**実装しない**。
- 議員の顔写真は**扱わない**。

## 技術方針

- フロントエンド: **素のJavaScript（フレームワークなし）**。**外部CDNに依存しない**（フォント・ライブラリも自前で同梱するか使わない）。
- ページはビルド時に事前生成した静的HTMLとする（各ページに固有URLとOGPを持たせるため）。JSはUI補助と検索にのみ使う。
- パイプラインは Python を想定（Shift_JIS の扱いとHTML解析のため）。依存ライブラリは最小限にする。
- スマホでの閲覧を最優先にデザインする。

## 絶対に守るルール

### 中立性・正確性
- AI要約には必ず「AI要約」と表示し、**原文（会議録の該当発言）へのリンク**を併記する。
- 議員・会派・町に対する**評価、点数付け、ランキング、賛否の色分けなどの価値判断をしない**。
- **議員個人の賛否を推測して表示しない**。会議録や公式資料に個人別の賛否が明記されている場合のみ表示する。
- 数字・固有名詞・答弁の結論は、原文と照合するチェック工程を通す。
- 全ページのフッターに「非公式・個人運営」「AI要約を含む」「正確な内容は原典を参照」を表示する。

### データ取得のマナー
- VOICES へのアクセスは**1リクエストごとに数秒の間隔**を空ける。並列アクセスはしない。
- 実行は日本時間の深夜帯。
- 取得した原文HTMLは `data/raw/` に保存し、同じページを何度も取りに行かない（取得済みはスキップ）。
- User-Agent に連絡先（運営者の問い合わせURL）を含める。

### AI要約の実行
- Claude API は **Batch API** を使う（即時性不要のため50%割引）。
- 要約は一度生成したら `data/summaries/` に保存し、**既存の要約は再生成しない**。再生成は明示的なフラグがある場合のみ。
- 要約JSONには使用モデル名・プロンプトのバージョン・生成日時を記録する。
- APIキーはリポジトリに含めない（GitHub Secrets の `ANTHROPIC_API_KEY`）。

### 公開フロー（半自動）
- 自動で本番公開しない。新しいデータは **Pull Request として生成**し、プレビューを人が確認して、マージしたら本番へデプロイする。

## 作業の進め方

- 設計変更や判断が必要な点は、**実装前に提案して確認を取る**こと。
- 各フェーズの完了条件は `docs/設計ドラフト.md` の「開発フェーズ」に従う。
- 画面の変更は、確認できるようにスクリーンショットまたはプレビューURLを添える。
